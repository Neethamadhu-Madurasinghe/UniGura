const imageUploadFormUI = document.getElementById('image-upload-form');
const imageUploadField = document.getElementById('actual-btn');

// imageUploadField.addEventListener('change', function() {
//     imageUploadFormUI.submit
// });